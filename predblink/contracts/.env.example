# Required for deployment and relayers
PRIVATE_KEY=0x_your_private_key_here

# Monad Testnet RPC (default is public endpoint)
MONAD_RPC_URL=https://testnet-rpc.monad.xyz

# Twitter API (optional — mock mode used if absent)
TWITTER_BEARER_TOKEN=

# BSCScan API key (only needed for BSC verification)
BSCSCAN_API_KEY=
